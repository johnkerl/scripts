#!/usr/bin/python -Wall

import sys
from sackmat_m import *

argc = len(sys.argv)
if (argc != 2):
	print >> sys.stderr, "Usage: %s {file name}" % (sys.argv[0])
	sys.exit(1)

z = read_column_vector(float, sys.argv[1])
Q = householder_vector_to_Q(z)
print_matrix(Q)
